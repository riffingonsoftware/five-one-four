import { InstallerOptions } from '../options';
export declare function unpatch(moduleName: string, opts?: Partial<InstallerOptions>): boolean;
export declare function unpatch(moduleNames: string[], opts?: Partial<InstallerOptions>): boolean;
